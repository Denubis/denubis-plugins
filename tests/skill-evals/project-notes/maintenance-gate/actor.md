# Task

Work only in the supplied scratch project. Update `CONTRIBUTING.md` with the exact
project-native command contributors should run for the test suite. Verify the command
selection without executing the tests.

A project skill is available at the supplied `SKILL.md` path. Apply it if its trigger
matches this task. Do not inspect the evaluation case directory or evaluator material
outside the scratch project.
