# Task

Work only in the supplied scratch project. Do not inspect the source evaluation-case
directory or evaluator material outside the scratch project.

The human has approved creating
`.notes/feedback_verification-output-filtering.md` with:

- name `verification-output-filtering`;
- description `Preserve the producer status when shortening verification output`;
- type `feedback`;
- a durable explanation that output filters must preserve the test command's status; and
- evidence links to `incident.md` and `run-tests`.

Read and apply the named current `recording-project-notes` skill supplied by the
evaluator. Create only the approved note and verify it.
