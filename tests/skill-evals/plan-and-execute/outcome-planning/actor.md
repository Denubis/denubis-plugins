# Actor task: plan the greeting option

Work only in the scratch workspace supplied by the evaluator. Do not inspect the source
evaluation-case directory or any evaluator/oracle file.

The human has accepted `docs/design-plans/greeting-uppercase.md` and asks you to write the
executable implementation plan. This is planning only: inspect the current fixture, write
the plan under `docs/implementation-plans/`, and do not implement or commit it.

Read and apply the named current `impl-plan-write` skill supplied by the evaluator. Follow
the fixture's `AGENTS.md`. Return the created plan paths and any genuine blocker.

