# Uppercase greeting design

**Status:** Current design

Add a keyword-only `uppercase` option while preserving the default greeting. Tests cover
both modes and README documents the public call. Before integration, the human should use
the finished function and judge whether the optional behavior is unsurprising at the
public boundary.

