# Test requirements

- `greeting.AC1`: `python -m unittest discover -p '*_tests.py' -q` exercises default and uppercase output and
  reports two passing tests. The uppercase test fails when the option is absent.
