# UAT requirements

- After all mechanical checks pass, import and call the finished public function in both
  modes. Judge whether the optional behavior is unsurprising and the documented call is
  sufficient to use it. Acceptance is falsified if the option changes default behavior,
  uppercases only part of the result, or requires information absent from README.

