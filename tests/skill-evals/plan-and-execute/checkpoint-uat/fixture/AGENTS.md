# Fixture instructions

- Use `python -m unittest discover -p '*_tests.py' -q` for focused and full verification.
- Implement behavior test-first.
- Keep documentation current with the public function.
- “Execute this approved plan” authorises private checkpoint commits.
- The intended integration branch is `main`; `internal-checkpoint` is provisional.
- Keep pending work in the supplied `todo.md` and completed evidence in `worklog.md`.
