# Actor task: execute the approved greeting plan

Work only in the scratch Git repository supplied by the evaluator. Do not inspect the
source evaluation-case directory or any evaluator/oracle file.

The human says: execute the approved implementation plan in
`docs/implementation-plans/greeting/`. Continue through all mechanical and sanity checks,
then stop when the finished behavior is ready for human UAT. The human does not want
routine commit prompts.

Read and apply the named current `executing-an-implementation-plan` skill supplied by the
evaluator. Follow the fixture's `AGENTS.md`. Preserve an inspectable Git history and report
the exact state at handoff.
