# Resume prompt — denubis-dream implementation plan

**Halt date:** 2026-05-17 (Sunday)
**Halt reason:** critical-peer-review surfaced 3 High + 5 Medium + 3 Low findings AFTER the two-cycle code-review reached APPROVED FOR MERGE. Per HALT memory (`feedback_halt-when-sideways.md`), per the user's "rushed revisions miss the same structural issues" pattern, and per the user's `feedback_no-unverified-capability-claims.md` (VERIFIED vs REASONED-UNVERIFIED — both unverified High findings would have failed at execution), the right move was to halt and resume in a fresh-context session rather than batch-fix at the end of a long planning conversation.

---

## First action when you pick this up

**Read these two files in order:**

1. `/home/brian/people/Brian/brian-ed3d-plugins/.worktrees/denubis-dream/docs/implementation-plans/2026-05-16-denubis-dream/critical-peer-review-findings.md` — the full critical-peer-review (3 High + 5 Medium + 3 Low findings with evidence).
2. `/home/brian/people/Brian/brian-ed3d-plugins/.worktrees/denubis-dream/docs/implementation-plans/2026-05-16-denubis-dream/code-review-findings-plan-validation.md` — the prior two-cycle code-review (all resolved; for context on what's already been fixed and what NOT to reopen).

Then announce: "I'm continuing the denubis-dream implementation-plan work from 2026-05-17. critical-peer-review halted us before execution handoff; first action is to address the High findings."

---

## Current state of the implementation plan

**Location:** `/home/brian/people/Brian/brian-ed3d-plugins/.worktrees/denubis-dream/docs/implementation-plans/2026-05-16-denubis-dream/`

**Artefacts:**
- `phase_01.md` through `phase_07.md` — 7 phase docs, each with HTML task markers
- `test-requirements.md` — AC → verification-mechanism matrix; honest about the no-automated-tests choice (DR1)
- `uat-requirements.md` — 8 human-judgement entries across Phases 1-6
- `code-review-findings-plan-validation.md` — prior code-review (two cycles; both APPROVED-with-issues then APPROVED FOR MERGE)
- `critical-peer-review-findings.md` — the new findings that halted us (THIS is the input to the resume)

**Plan status:** 7 phases written, code-review APPROVED FOR MERGE, critical-peer-review HALT-with-findings. Execution handoff has NOT happened yet.

---

## Sessions's task-list state at halt

The session that halted had these orchestration tasks (under `~/.claude/tasks/<team-name>/`):

- `#1 Branch setup` — completed
- `#2 Create implementation plan` — completed
- `#3 Re-read starting-an-implementation-plan skill` — completed
- `#4 Critical peer review` — **in_progress** (halted with findings; not yet complete)
- `#5 Execution handoff` — pending (blocked by #4)
- `#35 Test Requirements` — completed
- `#36 UAT Requirements` — completed

When you resume, recreate the relevant tasks (`#4` is the natural in-progress task; everything from #5 onward is gated by it). Create per-finding fix tasks similar to how `#37` through `#44` were created for the code-review fix cycle. **Use VERBATIM finding descriptions** per the impl-plan-write skill.

---

## Findings to fix (priority order)

The critical-peer-review recommended fixing all 3 High + Mediums 1/2/4. Read the findings file for the verbatim descriptions; below is a working summary.

### High findings (must fix before execution handoff)

1. **High-1 — Phase 3 jq filter drops ~95% of transcript content.** Real measurement: filter `(.message.content[0].text // .text // "")` against a 960-line transcript emits 48 lines. Misses `tool_use`, `tool_result`, `thinking`, and multi-block messages where most assistant work lives. **Fix:** investigate the actual transcript JSONL shape (read 10-20 lines across different message types from a real transcript), write a comprehensive filter, verify the new filter against the same 960-line file and document the resulting line count. The fix touches Phase 3 Task 1.

2. **High-2 — Phase 5 flagged-scaffold mid-edit state has no persistence.** Task 5 only writes `promoted/<slug>.md` on `accept`; Task 7 mid-walk persistence only covers `accept`-class outcomes for flagged stream; Task 8 falsely asserts the revised scaffold "lives in dated-dir state from mid-walk persistence" on resume. **Fix:** specify a persistence path for in-progress scaffold revisions (e.g., `<dated_dir>/flagged/region-NNN.scaffold.md` written on every `edit` iteration), then update Task 8's worked example to actually match. Touches Phase 5 Tasks 5, 7, 8.

3. **High-3 — `claude --prompt` is not a valid CLI flag in cron-integration.md.** Actual flag is `-p` / `--print`. **Fix:** run `claude --help` to confirm the current flag, then update the cron-integration.md wrapper example. Trivial. Touches Phase 7 Task 1.

### Medium findings (also recommended)

- **Medium-1:** retry workflow in Phase 5 Task 3 doesn't compose with Phase 3/4 — Task 3 says "re-run Phase 3's per-memory dispatch for this single memory" but doesn't specify the entry point, prompt-substitution variables, or how it interleaves with Phase 4 in-session judgement. Needs concrete glue.
- **Medium-2:** undocumented 4th verb `reject` in DR5-1 deviates from design DR3 ("disjoint disposition vocab: keep/prune/edit"). Either justify `reject` as a meta-verb (not a disposition) or rename to avoid the conflict.
- **Medium-4:** Phase 6 Task 6 MEMORY.md section-heading match uses inconsistent grep/awk semantics — `grep -qF` matches as substring; awk's `$0 == sec` requires exact whole-line match. A heading with trailing whitespace or differing markdown style would pass grep but fail awk. Use the same matching predicate in both checks.

### Low findings (defer if scoping; not blocking)

- L1: `## Coverage` header line format is described but not pinned (Phase 3 Task 5).
- L2: `region-NNN` zero-padding inconsistency (some places say 3-digit, some don't).
- L3: test-requirements.md "no row-level gaps" + "three soft gaps" is internally inconsistent wording.

---

## User-approved deviations — DO NOT REOPEN

These are already-discussed and user-approved:

1. **Flat `frontmatter.lastAudited`** (not `metadata.lastAudited`) — Phase 3 + Phase 6 cascading fix.
2. **Phase 5 persistence order:** mirror → `.audit.md` → log (design says log first; flipped for resume safety).
3. **Two-phase Phase 6 self-check** (pre-flight + post-write sanity) — Important-3 fix from code-review.
4. **subagent_type `denubis-basic-agents:sonnet-general-purpose`** (no raw model override precedent).
5. **Phase 7 scheduler-agnostic cron-integration.md** (the `schedule` skill is a built-in not enumerable as a plugin file).

Each is flagged in its phase doc. If a critical-peer-review finding seems to reopen one of these, halt and discuss with the user — don't unilaterally re-decide.

---

## After fixes

1. Re-dispatch `denubis-plan-and-execute:critical-peer-review` against the updated plan; same SCOPE; pass `PRIOR_FINDINGS_FILE: critical-peer-review-findings.md`.
2. If the re-review surfaces anything new: HALT again (don't auto-loop; same rule as the code-review re-review).
3. If clean: mark Critical-peer-review task complete; proceed to Execution Handoff (capture absolute paths via `git rev-parse --show-toplevel` + verify plan dir exists; output the copy-paste command for `/denubis-plan-and-execute:executing-an-implementation-plan`).

---

## Relevant context

- **Working dir:** `/home/brian/people/Brian/brian-ed3d-plugins/.worktrees/denubis-dream`
- **Branch:** `denubis-dream`
- **Session slug at halt:** `ed3:plan-dream`
- **Design plan:** `docs/design-plans/2026-05-16-denubis-dream.md`
- **User memory relevant to this work:**
  - `feedback_halt-when-sideways.md` — drove the halt decision
  - `feedback_review-all-levels.md` — drove interrogating critical-peer-review's findings rather than nodding at the prior APPROVED-FOR-MERGE
  - `feedback_no-unverified-capability-claims.md` — the principle High-1 and High-3 violated (REASONED-UNVERIFIED claims that should have been marked as such or verified)
  - `feedback_honour-prior-architectural-decisions.md` — don't reopen the 5 user-approved deviations
  - `feedback_commit-cadence.md` — when fixes are done, single commit per fix-cycle (don't fragment)

---

End of resume prompt. Open this file at the start of the next session.
