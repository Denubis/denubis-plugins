interface:
  display_name: "Claude Crash-Recovery Triage"
  short_description: "Inspect and recover interrupted Claude Code sessions"
  default_prompt: "Use $triage to inspect interrupted Claude Code sessions without pruning them silently."

policy:
  allow_implicit_invocation: false
