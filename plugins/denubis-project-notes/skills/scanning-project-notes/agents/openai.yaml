interface:
  display_name: "Use Project Notes"
  short_description: "Use project memory before and after work"
  default_prompt: "Use $scanning-project-notes to inspect relevant project memory before work and propose evidence-linked maintenance afterward."

policy:
  allow_implicit_invocation: true
